# Phase Four：空闲小剧场实施计划

状态：日常胶囊眼和唤醒挥爪已验证并提交（`4ab69d7`）；纯逻辑调度器及全部动作库已提交（`f096f35`），动作库完成首轮实机验证；空闲随机调度已接入并通过主机检查，待烧录实机验收；音乐陪伴尚未接入。以下实施记录中的“尚未”描述各次记录当时的状态。

## 已确认的产品边界

以 `prototypes/character-phase-four.html` 为视觉与动作基准，不重新设计眼睛、猫爪或道具。不增加主动说话、音效、云端调用，也不启动音乐陪伴模式。小剧场只在安静待机时发生；真实互动、音乐、错误和低电量提示优先，动作被打断后不续播。

当前统一自动动作池：托腮（较高权重）、吹泡泡、捉小鱼，不再区分安静/活泼档。躲猫猫、比心仍保留在动作库与测试入口，不加入自动动作池。揉眼睛用于困倦过渡，乐器属于后续音乐陪伴。

## 实施顺序

1. **纯逻辑调度器及测试。** 注入单调时钟和随机数，脱离 ESP-IDF 做确定性测试；暂不启用自动表演。
2. **移植动作。** 先吹泡泡、捉小鱼，再托腮、躲猫猫及困倦揉眼。沿用原型时间轴、入场/退场和视线联动，不用静态道具代替完整动作。通过调试入口逐个验证造型、帧率、恢复和中断，再启用随机调度。
3. **接入导演。** 增加低优先级空闲行为，由现有导演选定最终显示；渲染任务只执行选定动作，不自行覆盖聆听/音乐图层。动作自然结束返回胶囊眼。
4. **实机自动化与人工验收。** 自动验证调度边界、动作完成/中断/恢复、内存和帧率；人工确认实际视觉及麦克风/触摸链路。通过后再提交小剧场实现。

## 调度规则

- 仅保留开启/关闭，试用开关默认关闭。使用编译配置，不额外建设后台配置界面或修改 MCP。
- 首次等待 30–60 秒。动作结束或中断后至少休息 60 秒；等待与冷却同时约束，同一动作不连续出现。（2026-09-16 按用户反馈缩短等待并取消分档。）
- 5 分钟无用户互动后，候选等待间隔加倍；10 分钟无互动进入困倦，停止自动表演。自动表演本身不算用户互动。
- 当前固件 `ECHOEAR_IDLE_SLEEP_TIMEOUT_SECONDS=300`，与上述原型不同：只在小剧场试用模式统一采用 600 秒的视觉困倦边界；关闭试用仍保留原配置。不得改动硬件休眠、低电量关机策略。
- 忙碌时取消当前动作及待触发事件；回到稳定待机后重新等待，不补播积压事件。中断也计入冷却；不因状态重复通知清空历史动作或冷却时间。
- 音乐播放/暂停/缓冲、连接、聆听、说话、工具执行、错误/充电提醒、自检时不触发。无在场检测，不能据此认定儿童仍在设备旁。

## 渲染与资源约束

复用已验证的日常渲染任务和双缓冲，按需缓存静态部分，不为每个动作永久保留全帧缓存。姿态变更、退出和缓冲释放沿用同一互斥机制。失败时恢复原资源表情，避免反复申请失败。

把 `character_pose` 的试用整数标记收敛为命名动作枚举，显式区分循环待机和有限时长表演；明确结束时间和清理责任。几何、定时、调度分别测试，防止动作池扩展后数字映射混乱。

## 自动验收要求

- 固定随机种子验证等待范围、60 秒冷却、不连续重复、忙碌互斥、5/10 分钟边界及关闭/重开行为。
- 测试时推进虚拟时钟，不在发布固件缩短真实等待时间；实机加速仅通过默认关闭的调试入口开启，不改变动作速度。
- 每个动作至少 10 轮完整播放，分别在开始、中段、结束前软件中断，再恢复完整播放。不能把软件取消当作真实唤醒测试。
- 至少 20 fps 平均提交帧率作为初步性能目标，记录最慢帧、栈余量、缓存申请及释放后内存；不将提交帧率等同于屏幕刷新率。
- 首先取得真实待机 30–60 秒后自动触发的一次记录，再进行加速压力测试。高优先级接管无道具残留、无新旧眼形交叉，不为完成动作延迟语音收音。
- 场景回归：唤醒、触摸、连续对话、播歌、换歌、停止及自然结束；长时待机功耗另行测量。

## 本轮不包含

音乐节拍联动、乐器陪听、更多角色性格、后台远程配置、使用行为上报、唤醒模型调整及固件发布默认开启。

## 实施记录：纯逻辑调度器

新增 `main/boards/echoear/idle_theatre.h` 与 `docs/prototypes/test-idle-theatre.cc`。调度器不依赖 ESP-IDF，调用方提供单调毫秒时刻、随机数、模式、是否可表演，以及是否发生真实用户互动。输出当前命名动作、开始/停止边沿、困倦状态和下一次检查时刻；下一次检查可能是策略边界，不一定是动作开始时间。

已实现等待范围、托腮权重、不连续重复、自然结束、中断冷却、重复通知幂等、关闭/重新开启、忙碌后重新等待及 5/10 分钟边界。越过 5 分钟时，尚未开始的等待重新按降频区间安排；10 分钟困倦优先于当前小剧场，揉眼仅尝试一次，被打断后不续播，直到真实互动重置。迟到调用不补播旧动作；时钟回退被钳制，不能回退冷却时间。时间加法使用饱和处理。

```sh
clang++ -std=c++17 -Wall -Wextra -Werror -fsanitize=address,undefined \
  -I main/boards/echoear docs/prototypes/test-idle-theatre.cc \
  -o /tmp/echoear-idle-theatre-test
/tmp/echoear-idle-theatre-test
```

边界测试及固定种子 24 小时模拟通过（502 次表演），包括等待区间端点、冷却、权重与动作池、困倦抢占、取消后恢复及忙碌互斥。测试在主机虚拟时间运行，不代表设备持续运行了 24 小时。

当前调度器尚未被导演调用，没有更改设备的 300 秒困倦配置，没有自动随机表演，也没有新增固件开关或烧录。下一步是移植吹泡泡与捉小鱼的绘制及入退场，先经调试入口验收，再接入自动调度。

## 实施记录：吹泡泡与捉小鱼独立预览

已新增 `CharacterPreview::kBubble`（7 秒）和 `kFish`（6.5 秒），时长取自原型。泡泡包含举棒、长大、飘走、破裂和收爪；小鱼包含游动、逃离、扑捉和视线联动。保持竖直眼形，通过双眼间距和远近尺寸表示侧视。使用透明度合成与时间曲线处理道具入退场；原型依赖帧历史的视线平滑改为确定性的时间缓动，尚不能视为逐像素等价移植。

在已启用 `ECHOEAR_CHARACTER_TEST_SERIAL` 的调试固件中发送 `character-test theatre`，只播放这两段；`character-test start` 和语音“测试表情”仍是原来的三段，不改变既有自动化基线。沿用原取消、音乐互斥和结束恢复路径；这两段不申请吉他静态缓存，结束恢复当前真实状态。

主机脚本新增独立套件：

```sh
python scripts/character_serial_test.py --port /dev/cu.usbmodem1201 \
  --suite theatre --rounds 10 --output /tmp/echoear-theatre-suite.jsonl
```

该套件将运行 10 次两段完整播放、两个阶段中断及一次完整恢复，共 13 项。`scene=3/4` 分别对应泡泡和小鱼。暂不通过该命令启用随机表演。

主机 ASan/UBSan 检查覆盖五个场景完整时间轴共 950 帧，并保留 540 帧吉他缓存等价验证；新动作在 0 秒、结束时及超时后的画面与中性胶囊眼一致，确认没有残留道具。已抽查导出静态帧，动态视觉及实机帧率仍需烧录后确认。此轮尚未烧录、提交，也未接入随机调度。

### 两段动作实机自动验收（2026-09-14）

已烧录并校验应用分区，保留网络和资源配置；执行 `--suite theatre --rounds 10`，报告为 `/tmp/echoear-theatre-suite-20260914.jsonl`。13 项全部通过：10 轮两段完整播放、两个阶段软件中断、一次完整恢复。所有完整轮次 `completed=2 failed=0 cancelled=0`；两个中断轮分别 `completed=0/1 cancelled=1`，随后可正常重新启动。

| 场景 | 完整轮次平均提交帧率范围 |
| --- | --- |
| 吹泡泡 | 20.28–20.62 fps |
| 捉小鱼 | 20.87–21.33 fps |

任务栈最小剩余 6316 字节。第 1 轮接近启动阶段，内存变化混有服务初始化；第 2 轮结束 PSRAM 为 13611256 字节，后续有波动但无持续下降，最后恢复轮为 13611236 字节（相差 20 字节）。短时观察没有发现随轮次持续累积的下降，不据此断言不存在任何内存泄漏。

日志可见各轮自检退出后恢复待机胶囊眼；测试结束主机释放串口。动态视觉仍等待用户确认，未将软件中断替代实际触摸/语音测试；随机调度仍未接入，本轮未提交或推送。

用户随后确认泡泡、小鱼视觉没有异常，同意一次补齐剩余动作。

## 实施记录：补齐原型动作库

新增七个命名动作：托腮（5 秒）、揉眼（5.4 秒）、比心（4.8 秒）、躲猫猫（6 秒）、沙锤/小鼓/小琴（各 7 秒）。至此渲染器覆盖 11 个原型动作和待机胶囊眼，但不等于自动随机调度或真实音乐陪伴已经接入。

- 托腮保留先抬爪再靠脸、先回正再收爪；B 型托腮爪镜像朝外，头部整体倾斜而非单眼各自旋转。
- 揉眼保留眯眼、抬爪、轻揉、收爪，以困倦眼形结束。调试测试结束仍恢复真实设备状态，不人为把设备改为休眠。
- 比心使用捧心爪及轻微心跳；躲猫猫包含双爪遮挡、单侧打开偷看和开心收场。
- 沙锤、小鼓、小琴使用开心弯眼、预设时间轴及少量音符；尚未与真实音频节奏或能量联动，不生成声音。
- 新增眼形轮廓插值、托腮/捧心爪、圆角矩形等绘制原语；旧吉他缓存和原三段预览保持兼容。

串口入口新增 `character-test remaining` 和 `character-test all`。前者播放托腮、比心、躲猫猫、沙锤、小鼓、小琴、揉眼，约 42.2 秒；后者在此之前加入胶囊眼、挥爪、吉他、泡泡、小鱼，总计约 73.7 秒。揉眼放在末尾避免困倦眼形在表演中间突变。原 `start`、`theatre` 及语音测试入口不变。

```sh
python scripts/character_serial_test.py --port /dev/cu.usbmodem1201 \
  --suite remaining --rounds 10 --output /tmp/echoear-remaining-suite.jsonl

python scripts/character_serial_test.py --port /dev/cu.usbmodem1201 \
  --suite all --rounds 1 --output /tmp/echoear-all-suite.jsonl
```

脚本按各套件真实时长设置截止时间，不再使用统一 30 秒超时；各套件仍检查每个阶段的软件取消及完整恢复。`remaining --rounds 10` 为 18 项，`all --rounds 1` 为 14 项，实际运行时间包含中断前的各段播放，明显长于单轮预览。

主机检查覆盖 2223 帧完整渲染时间轴及 540 帧吉他缓存逐字节比较，验证缓存不变、越界保护、alpha、非法输入及新动作的入退场。除揉眼保留困倦眼形外，新动作端点与中性胶囊眼一致；揉眼末尾下半屏无手爪残留。已检查七种动作的导出静态关键帧。尚未烧录本轮补全版本，实机性能和动态视觉仍待验收；未开启自动随机表演或音乐陪伴，也未提交、推送。

### 全动作库首轮实机验收（2026-09-14）

已烧录并校验完整动作版；运行 `--suite all --rounds 1`，原始报告 `/tmp/echoear-all-suite-20260914.jsonl`。14 项流程检查全部通过：首次完整播放 12 个场景，逐阶段进行 12 次软件中断，再完整播放一次；两次完整轮均 `completed=12 failed=0 cancelled=0`，各中断轮完成场景数与目标一致，结束状态查询均为 `running=0`。用户观看后确认视觉效果可以保留。

两次完整轮中的新动作平均提交帧率范围（不是屏幕实际刷新率）：

| 动作 | fps |
| --- | --- |
| 托腮 | 21.92–22.23 |
| 揉眼 | 24.36–24.37 |
| 比心 | 21.83–21.86 |
| 躲猫猫 | 21.10–21.44 |
| 沙锤 | 20.12–20.18 |
| 小鼓 | 19.75–19.78 |
| 小琴 | 19.77–19.80 |

小鼓、小琴略低于 20 fps 初步目标；用户视觉认可不代表该性能目标已严格达成，保留后续优化项。任务栈最小剩余 5560 字节。首轮结束 PSRAM 13610776，最终恢复轮结束 13608948，相差 1828 字节；期间有上下波动，不能仅凭本次结果判定不存在内存泄漏，长时/多轮稳定性仍需进一步观察。

测试结束串口已释放。此测试只验证调试预览及软件中断恢复，不替代所有动作的真实触摸/语音测试；随机调度和真实音乐陪伴仍未开启。提交和推送状态以 Git 记录为准。

## 实施记录：接入日常空闲调度（2026-09-15）

新增 `ECHOEAR_IDLE_THEATRE_TRIAL`，仓库默认关闭，依赖日常角色试用与导演。开启时默认安静档；`ECHOEAR_IDLE_THEATRE_LIVELY` 可切换活泼档。此次本地忽略的 `sdkconfig` 已开启安静试用用于构建验证，不代表发布默认开启或设备已经升级。

导演使用原有单次定时器驱动纯调度器，统一决定动作开始、自然结束和高优先级中断。复用日常任务和双缓冲，`character_pose` 改为可选命名枚举，不再通过整数 0/1 隐式映射。小剧场不循环；动作结束返回胶囊眼，困倦揉眼结束进入原有睡眠表情。试用关闭时保留旧的 300 秒视觉困倦路径，未修改硬件休眠或电量策略。

唤醒、显式触摸入口和非空用户识别文本重置真实互动计时；自动进入聆听、重复待机状态、云端表情、动画播放都不算用户互动。自检显式阻塞调度，结束重新等待；音乐缓冲/播放/暂停、对话、工具、错误及低电量提醒通过导演现有优先级抢占。被中断动作不续播。

新增实际 `expression_director.cc` 的主机集成测试，只有 ESP 定时器、随机数、应用队列和日志使用替身，不复制导演算法。命令：

```sh
bash docs/prototypes/test-expression-theatre.sh
```

测试覆盖关闭/安静/活泼三种构建组合、首次真实策略等待、重复待机不推迟动作、有限动作结束、冷却、自检阻塞、唤醒/聆听接管、音乐播放/缓冲/暂停、云端情绪、充电提示、10 分钟困倦和互动恢复、定时器创建失败，以及调度时长与渲染时长一致性。使用虚拟时钟和同步应用队列，不代表通过真实麦克风链路、线程并发或持续实机测试。

纯调度器 ASan/UBSan 测试、原型 Node 回归与安静试用固件构建已通过。下一步仍须烧录后取得一次真实 90–180 秒自动触发记录，随后验证触摸/语音中断、播放/换歌/停止回归及长时稳定性；本轮未烧录，不将上一版动作预览验收作为本版自动调度验收。

### 安静试用首次自然触发（2026-09-15）

已确认 ESP32-S3 设备 MAC `90:e5:b1:cc:bd:44`，仅向应用分区 `0x310000` 写入 2788848 字节固件，写入哈希校验通过并重启；未重写 NVS、模型、资源或 OTA 选择数据。

串口记录 `/var/folders/xk/61y0rx650b1b6pf9wyc5p_tr0000gn/T/echoear-idle-live-i0l18i7u.log` 中，启动后 `109114 ms` 出现 `Theatre act=2 started=1 ... eligible=1`，随后 `Character live pose=3`（吹泡泡）；`116124 ms` 自然结束，`116134 ms` 恢复 `pose=0`（胶囊眼）。实机动作持续约 7 秒，无加速或手动预览触发。监听开始于重启后，未捕获最初进入待机的时间，因此这里只报告启动相对时间，不将其直接当作精确的待机等待时长。

截至该次自然结束，采集日志未见崩溃、看门狗或渲染失败。实际视觉、真实唤醒/触摸中断、音乐回归及长时稳定性仍待验收，不能视为全部测试通过。

### 取消分档、缩短等待（2026-09-16）

用户反馈小剧场等待过久，批准等待 30–60 秒、结束或中断后冷却 60 秒，并明确不区分安静与其他档位。删除活泼编译选项和策略分支，只保留开关，沿用当前托腮/泡泡/小鱼动作池及 2:1:1 初始权重。5 分钟后等待翻倍为 60–120 秒，10 分钟困倦；高优先级互斥与不连续重复保持不变。旧浏览器原型作为历史视觉基准，调度参数以本节和固件为准。

同步更新真实导演的开启/关闭两组集成测试及纯调度器边界测试。ASan/UBSan 全部通过；24 小时虚拟时间、固定随机种子、交替开启/关闭模拟产生 567 次动作（不代表实机连续运行）。更高触发频率的实机功耗与长时稳定性尚未测量。

固件构建通过；仅烧录应用分区 2788800 字节，哈希校验通过。记录 `/var/folders/xk/61y0rx650b1b6pf9wyc5p_tr0000gn/T/echoear-unified-idle-kst1qvu8.log` 显示启动后 `48695 ms` 自动吹泡泡，`55705 ms` 自然结束，`55715 ms` 返回胶囊眼。监听未包含最初待机事件，故报告启动相对时间而非精确待机等待时长；冷却边界已通过主机测试，后续继续实机观察。未提交、推送本轮改动。
